# Tasks: Deployment README

## 1. Инфраструктура деплоя

- [x] 1.1 `docker/Dockerfile.orchestrator` — multi-stage: stage 1 `rcktsci/java-tooling:25` (корпоративный образ: Liberica JDK 25 alpine-musl + Maven внутри) + `mvn -DskipTests package`; stage 2 `bellsoft/liberica-openjre-alpine-musl:25` + boot-jar, `ENTRYPOINT java -jar`. Верификация: `docker build -f docker/Dockerfile.orchestrator -t spring-harness:local .` проходит локально, `docker run --rm spring-harness:local java -version` печатает 25.x.
- [x] 1.2 `docker-compose.yml` — сервисы `postgres:17` (том `pgdata`; env `POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB: harness` — иначе БД не создастся; healthcheck `pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}`) и `orchestrator` (build из 1.1, `depends_on: postgres: condition: service_healthy`, монтирование `/var/run/docker.sock` и `HARNESS_WORKSPACE_ROOT` → одинаковый путь в контейнере; env-блок orchestrator: `DB_*`, `KEYCLOAK_ISSUER_URI`, `KEYCLOAK_JWKS_URI`, `HARNESS_WEBHOOK_SECRET`, `HARNESS_WEBHOOK_BASE_URL=http://<vm>:8080`, `HARNESS_LLM_KEY_V1`, `HARNESS_WORKSPACE_ROOT`, `MANAGEMENT_SERVER_PORT=8081` — секреты-плейсхолдеры `CHANGE_ME`); сеть по умолчанию, publish только 8080 (API) и 8081 (actuator). Верификация: `docker compose config` валиден без warnings о missing env.

## 2. README

- [x] 2.1 Корневой `README.md`, раздел «Бэкенд»: пререквизиты VM (docker + плагин compose, доступ к sso.rocketscien.se и hub.docker.com), подготовка (`mkdir -p /srv/harness/workspaces` — bind-mount-каталог создать заранее **и отдать его пользователю контейнера оркестратора**: оркестратор сам создаёт `workspaces/<sessionId>` через `Files.createDirectories`, root-owned корень даёт `AccessDeniedException` на первом вызове инструмента; uid контейнера НЕ пинится в compose — ему нужен доступ к `/var/run/docker.sock`, см. доработку 2.5), шаги — clone → собрать helper-образ (`docker build -f docker/Dockerfile -t harness-helper:local .`) → при необходимости pre-build jar на машине разработки (`chmod +x mvnw; ./mvnw -DskipTests package`) вместо in-container build (альтернатива из design Risks) → заполнить env в compose (таблица: имя, назначение, пример; обязательные DB_*/KEYCLOAK_*/HARNESS_*; запрет коммитить секреты, при желании `git update-index --skip-worktree docker-compose.yml`) → `docker compose up -d` → проверка (`/actuator/health` на 8081, логи контейнера, Liquibase накатился). Верификация: текст присутствует, команды копипастой выполняются на VM (первый прогон владельца).
- [x] 2.5 Живой стенд 2026-09-24: bash-инструмент падал с `Failed to prepare workspace directory <path>` — README утверждал «root-owned is fine, оркестратор туда не пишет», что неверно (`WorkspaceContainerManager.createAndStart`). Фиксы: инструкция chown в README + комментарии compose, в текст ошибки добавлен тип корневой причины (`rootCause`) вместо повтора пути, тест `workspaceMountFailureYieldsError` требует наличия типа причины. Верификация: на VM `bash OK` в живом Turn, локально целевой тест зелёный.
- [x] 2.2 README, раздел «Keycloak» (realm `rcktsci`): клиент десктопа — public + Standard flow + PKCE S256 + redirect `http://127.0.0.1/*` + Web Origins `http://127.0.0.1` (уже заведён владельцем — зафиксировать как чеклист для воспроизведения); mapper Group Membership → claim `groups` (full path off) на client-scope клиента; пользователь — член группы `harness-users`; предупреждения: без маппера — 403 на всё; не менять realm-дефолты. Верификация: чеклист воспроизводим с нуля на тестовом realm.
- [x] 2.3 README, раздел «Клиент (Web Desktop)»: пререквизиты — Node.js ≥20.18.0, pnpm ≥10 (или corepack); два пути — dev (`cd web-desktop && pnpm install && pnpm dev`, Settings: baseUrl = `http://<vm>:8080`, issuer = `https://sso.rocketscien.se/realms/rcktsci`, clientId; Login) и упакованный (упаковка выполняется на целевой ОС: `pnpm build && pnpm package:win` — на Windows-машине, `package:linux` — на Linux; дистрибуция по operations.md §6); проверка первого сценария (логин → создание сессии → отправка → ответ); предупреждение: без валидного `HARNESS_LLM_KEY_V1` сервер стартует, но Turn'ы падают; ссылки: operations.md, web-desktop-client.md, web-desktop/docs/smoke.md. Верификация: сценарий выполняется владельцем локально против VM.
- [x] 2.4 README, шапка: 3 строки о проекте (агентный harness: сессии, workflow, субагенты, MCP, десктоп-клиент), стек, ссылки на docs/design/* и AGENTS.md. Верификация: читается как точка входа, без дублей содержимого доков.

## 3. Верификация и закрытие

- [ ] 3.1 Прогон владельца на VM по README: compose поднялся, health зелёный, KC-логин проходит, сессия отвечает, bash-инструмент исполняется в helper-контейнере. Расхождения текста с реальностью — правки в README в рамках этого change. Верификация: владелец подтвердил прогон.
- [ ] 3.2 `openspec validate deployment-readme --strict`; коммит оркестратора после ревью-цикла. Верификация: validate — valid.
