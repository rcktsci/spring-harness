# Tasks: CI: Backend Image

## 1. Контекст сборки

- [ ] 1.1 Создать `.dockerignore` в корне: исключить `.git/`, `target/`, `web-desktop/{node_modules,dist,out,test-results,playwright-report}/`, `.idea/`, `.vscode/`, `.opencode/`, `docs/`, `openspec/`, `*.log`; `api/` и `src/` в списке исключений НЕ должны присутствовать — проверка: `docker build -f docker/Dockerfile.orchestrator -t spring-harness:ci-check .` собирает boot-jar (openapi-generator читает `api/openapi.yaml`, git-commit-id не падает без `.git`)
- [ ] 1.2 Проверить чистую сборку с нуля: `docker build --no-cache -f docker/Dockerfile.orchestrator -t spring-harness:ci-check .` завершается успешно, в образе только `app.jar` + JRE — проверка: вывод шага сборки без ошибок, `docker run --rm --entrypoint sh spring-harness:ci-check -c "ls /app"` показывает только `app.jar`

## 2. Workflow

- [ ] 2.1 Создать `.github/workflows/backend-image.yml`: триггеры `push` (branches: `main`, tags: `v*`) и `workflow_dispatch`; `permissions: contents: read, packages: write`; `concurrency: backend-image-${{ github.ref }}` с `cancel-in-progress: true` — проверка: файл валиден YAML, триггеры и права читаются из файла
- [ ] 2.2 Шаги workflow: `actions/checkout@v7` → `docker/setup-buildx-action@v4` → `docker/login-action@v4` (GHCR, `${{ secrets.GITHUB_TOKEN }}`) → `docker/metadata-action@v6` (images: `ghcr.io/${{ github.repository_owner }}/spring-harness-orchestrator`; `flavor: latest=false`; tags: `type=ref,event=branch`, `type=sha,format=short`, `type=semver,pattern={{raw}}`, `type=semver,pattern={{version}}`, `type=raw,value=latest,enable={{is_default_branch}}` — `{{major}}`/`{{minor}}` не используются: на `0.x` тег `0` вводит в заблуждение) → `docker/build-push-action@v7` (`file: docker/Dockerfile.orchestrator`, `context: .`, `platforms: linux/amd64`, `push: true`, `cache-from: type=gha`, `cache-to: type=gha,mode=max`) — проверка: каждый action зафиксирован на существующей мажорной версии, в шаге сборки явно заданы `push: true`, `platforms: linux/amd64` и оба ключа GHA-кэша
- [ ] 2.3 Добавить шаг гейтов, выполняемый только на теговых прогонах (`if: startsWith(github.ref, 'refs/tags/v')`): (1) версии извлекаются разбором XML-дерева `pom.xml` (`python3`, элемент `<version>` верхнего уровня, а НЕ первый `<version>` в файле — первый принадлежит `<parent>` `spring-boot-starter-parent:4.1.1`, pom.xml:7-12) и ключом `version` из `web-desktop/package.json`; обе должны равняться версии тега без префикса `v`, иначе — fail с сообщением о расхождении; (2) если тег уже существует в GHCR (`docker buildx imagetools inspect ghcr.io/${{ github.repository_owner }}/spring-harness-orchestrator:${TAG}` завершается успешно) — fail «релизный тег неизменяем» (D-CI16) — проверка: локально повторить логику шага (а) на заведомо неверной версии в `pom.xml` и убедиться в падении; (а) на текущем `pom.xml` (`1.0.0-SNAPSHOT`) шаг выдаёт именно `1.0.0-SNAPSHOT`, а не `4.1.1` версии parent'а; на ветке шаг не выполняется
- [ ] 2.4 Прогнать workflow на первом пуше: образ публикуется в GHCR с тегами `latest`, `main`, `sha-<short>` и OCI label'ами — проверка: в GHCR виден пакет `spring-harness-orchestrator` с тремя тегами, `docker buildx imagetools inspect ghcr.io/rcktsci/spring-harness-orchestrator:latest` показывает label'и `org.opencontainers.image.revision`/`source`

## 3. Документация и приёмка

- [ ] 3.1 Обновить `README.md`: раздел про доставку образа на VM — после первого пуша владелец переключает пакет GHCR на публичный, дальше `docker pull ghcr.io/rcktsci/spring-harness-orchestrator:latest` без логина, `docker tag ... spring-harness:local`, запуск через существующий `docker compose up -d`; отметить, что локальный `build:` в compose остаётся рабочим путём — проверка: инструкции воспроизводимы по README без обращения к внутренним докам, и в README нет шага с PAT
- [x] 3.2 Архитектура VM: `x86_64`, подтверждено владельцем 2026-09-24 — `platforms: linux/amd64` в workflow, дополнительных проверок не требуется
- [ ] 3.3 Приёмочный прогон на VM: `docker pull` опубликованного образа, `docker compose up -d`, `curl http://<vm>:8081/actuator/health` отвечает `UP` — проверка: healthcheck зелёный, в `docker images` тег `spring-harness:local` указывает на скачанный образ
- [ ] 3.4 Проверить откат: `docker pull ghcr.io/rcktsci/spring-harness-orchestrator:sha-<предыдущий>` поднимается и проходит healthcheck — проверка: healthcheck зелёный на предыдущем коммите
- [ ] 3.5 Зафиксировать в `docs/design/decisions.md` новый ADR (**D-96** — D-94 занят Jackson 2, D-95 занят host-грантами deployment-readme) с решениями D-CI1…D-CI16 и отвергнутыми альтернативами; обновить `AGENTS.md` (M5/deploy status + новый CI + релизная политика) — проверка: вручную — ADR D-96 содержит отвергнутые альтернативы, `AGENTS.md` содержит новый пункт; плюс `openspec validate ci-backend-image --strict` зелёный (внешний openspec CLI, команду выполняет оркестратор, не артефакт репозитория)

## 4. Релизная политика

- [ ] 4.1 Создать `CHANGELOG.md` в корне по формату Keep a Changelog: заголовок, легенда сравнения ссылок, раздел `0.1.0` с датой релиза и перечнем того, что вошло в первую версию (M1–M5, deployment-readme, CI) — проверка: файл существует, формат соответствует Keep a Changelog, запись `0.1.0` перечисляет состав релиза
- [ ] 4.2 Описать релизную процедуру в `README.md`: бамп-правила (MAJOR = breaking `api/openapi.yaml`/WS/SSE, MINOR = новое API, PATCH = баги), правило «пока версия `0.x`, breaking = MINOR с пометкой `**BREAKING**` и миграцией в changelog», правило «тег ставится только на коммите, уже в `main`», правило «релизный тег неизменяем — перезапись = следующая версия» (D-CI16), шаг «правишь версию в `pom.xml` и `web-desktop/package.json` + пишешь changelog → коммит → `git tag vX.Y.Z && git push origin vX.Y.Z`» — проверка: инструкция воспроизводима по README, в ней явно присутствуют все четыре правила и нет шага с PAT или с автогенерацией changelog
- [ ] 4.3 Добавить в `README.md` команду «какая версия на VM» через OCI label'ы (`docker inspect` контейнера: `org.opencontainers.image.revision` — всегда коммит, `org.opencontainers.image.version` — semver на релизном образе, `main` на образе из push в ветку) и процедуру обновления/отката по semver-тегу — проверка: команда даёт версию на текущем стенде, инструкция отката совпадает с фактическими тегами в GHCR
- [ ] 4.4 Первый релиз `v0.1.0` (после успешного прогона 2.4 и приёмки 3.3): оркестратор готовит релиз-коммит (версии `0.1.0` в `pom.xml` и `web-desktop/package.json` + запись в `CHANGELOG.md`), владелец аппрувит diff; затем тег `v0.1.0` пушится в `origin` — проверка: теговый прогон CI зелёный и публикует теги `v0.1.0` и `0.1.0`; в логах прогона видно, что гейты 2.3 сверили обе версии с тегом и тега ещё не было в GHCR; негативная проверка гейтов (заведомо неверная версия в pom → падение, повторный пуш того же тега → падение) проводится на временном теге/локально до боевого `v0.1.0`
- [ ] 4.5 Проверить негативный сценарий релизной дисциплины: пометка `**BREAKING**` и запись в changelog обязательны для breaking-изменений (D-CI14) — проверка: ревью-инструкция в change/PR-шаблоне либо в README упоминает требование пометки; автоматической блокировки в CI нет и это зафиксировано
