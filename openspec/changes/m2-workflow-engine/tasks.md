## D. Контракт-first: спека M2 (заморозка)

- [ ] D.1 Спроектировать OpenAPI-подмножество M2 (api-contracts §4): endpoints `tasks`, `tasks/{id}/{subtasks,suspend,resume,stop,dependencies,history,comments,tree,events}`; `workflows`, `workflows/{key}/revisions`; `triggers`; `webhooks/tasks/{taskId}/{token}`, `webhooks/triggers/{triggerId}/{token}`. Схемы: TaskDto, TransitionDto, CommentDto, TaskTreeNode, WorkflowDto, WorkflowRevisionDto, TriggerDto, WebhookError. Каталог ошибок: единственный новый код `wrong-transition` (409), остальные M2-коды уже в §6. SSE `tasks/{id}/events` (api-contracts §3.2 — `task.transition`/`task.status`/`subtask.terminal`/`task.comment`+ping; курсор `task_event_seq`). Слияние с M1-операциями: расширение `session-api` (PATCH params иммутабельны; `runtimeStatus: IDLE|TURN_RUNNING|PARKED_ASYNC|PARKED_CLIENT`; SessionDto дополнен `kind`/`taskId`/`stateCode`; `GET /api/v1/sessions/{id}/tree` — `taskId`+`stateCode` для STATE-узлов) и поверхности `agent-turn` (инструмент `transition(taskId, toState, kind, reason)` + гейт metaTools — фиксация в спеке; REST-генерации не требует). Полный ревью-цикл 3 ревьюера (GLM-Flash + DeepSeek-Flash + Mercury) → консенсус → судья → заморозка.
- [ ] D.2 Регенерация из замороженной спеки + добавление `jackson-annotations:provided` (компиляционная нужда генератора; main-runtime = Jackson 3) → `mvn clean verify` зелёный → re-approve 3 ревьюеров. Генерация покрывает расширения `session-api` (обновлённый SessionDto, SSE-контроллер `tasks/{id}/events`, регенерированный `GET /api/v1/sessions/{id}/tree` — регенерация заставит обновить контроллер).

## H. БД: миграции + WorkflowRegistry/TaskRegistry (CRUD без движка)

- [ ] H.1 Миграции `010_create_table_workflow.xml`, `011_create_table_workflow_revision.xml` (UNIQUE `(workflow_id, rev)`, FK→workflow), `012_create_table_task.xml` (колонки data-model §4 + индексы: `(parent_task_id, status_projection)`, GIN `(tags)`, partial `current_state_kind = 'WAIT_TASKS'` (POLL-скан; WAIT_WEBHOOK пассивен — только таймаут-скан), partial `deadline_at IS NOT NULL`, partial `(current_state_kind='AGENT' AND status_projection='RUNNING')` для bootstrap-скана), `013_create_table_task_dependency.xml` (PK пары + цикл-валидация на уровне приложения + `INDEX (blocked_task_id)` для переоценки WAIT_TASKS), `014_create_table_task_comment.xml` (append-only), `015_create_table_task_transition_history.xml` (append-only, индекс `(task_id, created_at, id)` — курсор истории), `016_create_table_trigger.xml` (`revoked_at`). Колонки `session.task_id`/`state_code`/`agent_revision_id` и PARTIAL UNIQUE `(task_id, state_code) WHERE kind='STATE'` — **уже в миграции 005 (M1): не создавать, переиспользовать**. Запись в `changeset-master.xml`. Verify: `mvn -DskipTests clean install` → Liquibase отрабатывает без ошибок.
- [ ] H.2 Реализовать `WorkflowRegistry` (Java-интерфейс + `impl/WorkflowRegistryImpl`): `createWorkflow(key, name, graph) → WorkflowRevision`; `newRevision(workflowKey, graph)`; `get(workflowKey)`; `getRevision(workflowKey, rev)`; `list(...)` (cursor-пагинация по `createdAt` desc); `WorkflowGraphSchemaValidator` (правила §2: уникальность codes, fan-out — ≤1 ребро каждого kind, ≥1 исходящий, BASH/WAIT обязаны иметь и ERROR, и TIMEOUT, достижимость TERMINAL, JSON-Schema контракта — ограниченный профиль D-58). Errors → массив `{ pointer, rule, message }`. Verify: unit-тесты валидатора на позитивные/негативные графы (≥10 кейсов, включая негативный «WAIT без ERROR»).
- [ ] H.3 Реализовать `TaskRegistry` (Java-интерфейс + `impl/TaskRegistryImpl`): `createTask(workflowRevisionId, title, description, owner, parent?, params, tags) → Task` (с валидацией `paramsSchema`); `get(id)`; `patch(id, mergePatch)` (`params` иммутабельны); `addDependency(blockerId, blockedId)` (DFS-валидация циклов + self-loop); `removeDependency(blockerId, blockedId)`; `suspend(id, cascade)`, `resume(id)`; `stop(id)` (атомарный CAS в `'$CANCELLED'`); `addComment(id, author, body)`; `list(parent?, status?, mine?, tags?, q?, cursor)`; `getTree(id, depth?)`; `getHistory(id, since?, limit?)` (курсор `(created_at, id)`). Verify: unit-тесты (валидация циклов, иммутабельность params, idempotency suspend/resume/stop) + интеграционные тесты (живой Postgres, поведение с реальной схемой). ArchUnit-правило расширено параметризацией на 4 модуля (`workflow`/`task` добавлены).

## I. Движок состояний (BASH/WAIT_WEBHOOK/WAIT_TASKS)

- [ ] I.1 Реализовать `TaskEngine` (Java-интерфейс + `impl/TaskEngineImpl` — пакет `execution.impl`, поверх контракта `TaskRegistry`): точка входа — `processTaskTransition(taskId, fromState, toState, kind, reason)` (атомарный CAS: `UPDATE task SET current_state=next, status_projection=…, current_state_kind=…, deadline_at=…, updated_at=now() WHERE id=? AND current_state=expected AND suspended=false`; транзакция: INSERT в `task_transition_history` + обновление `task` + пересчёт `status_projection`/`current_state_kind`). Stop — CAS без гварды suspended. Конфиг: `harness.task.transition.kind-timeouts.{bash,wait-webhook,wait-tasks,agent}.default` (для `state.timeout`). Verify: unit-тест CAS-атомарности (двойной transition — один выигрывает); интеграционный тест stop-vs-transition.
- [ ] I.2 Реализовать `BashStateExecutor` (`execution.impl`, поверх контракта `WorkspaceTools`): `execute(taskId, stateCode, script, workspace, attempt)` → запускает `WorkspaceTools.executeBash(...)` в отдельном контейнере `harness-task-<taskId>`; контейнер задаётся через workspace-биндинг (task-контейнер — см. спеку workspace-tools). Перед запуском инкрементирует `state_attempt`. По завершении: `exit=0` → переход NEXT; `exit≠0` → ERROR; таймаут (превышение `state.timeout`) → TIMEOUT. Reason: `{ exitCode, stdout (truncate по конфигу), stderr, durationMs, attempt }`. Скрипт-инвариант «идемпотентность» — Javadoc. Verify: unit-тест парсинга exit; интеграционный тест с реальным контейнером (write_file → `wc -l` → SUCCESS; `false` → ERROR; убийство → TIMEOUT).
- [ ] I.3 Реализовать `WaitWebhookStateExecutor` (passive; handler — пачка L.3, здесь только контракт и состояние): `onWebhookArrived(taskId, payload, source)` → CAS переход NEXT (или ERROR при валидации ограниченного профиля `payloadSchema` — D-58) с `reason_jsonb = { kind:'webhook', source, payloadSummary }`. Метод вызывается webhook-handler'ом (см. L.3). Verify: unit-тест валидации схемы; интеграционный тест через webhook-handler.
- [ ] I.4 Реализовать `WaitTasksStateExecutor`: `reevaluate(taskId)` → определить scope (`ALL_CHILDREN|BLOCKED_BY|TAGGED(x)|EXPLICIT(...)`) — выполнить SQL-запрос по индексу; condition (`ALL_TERMINAL|ALL_SUCCESS`); если выполнено → переход NEXT с reason `{ closedBy: [taskIds] }`; если `ALL_SUCCESS` + хоть один FAILED/CANCELLED → ERROR. Триггер переоценки: событие терминала подзадачи (через InProcessTaskWakeBus — см. I.5), изменение тегов (триггер шинного события при patch с tags), изменение blocked_by. Verify: unit-тест логики scope/condition; интеграционный тест «дерево подзадач → WAIT_TASKS закрывает».
- [ ] I.5 Реализовать `InProcessTaskWakeBus` (аналог M1 `InMemorySessionEventBroadcaster`, событийный канал для задач; Javadoc: это внутренняя шина wake движка — bootstrap/переоценка/терминал, **не** SSE; SSE-канал задачи — `TaskWakeBroadcaster`, пачка J.4): `publishTaskTerminal(taskId)`; `publishTaskWake(taskId)`; `subscribeTaskWake(handler)`. ShedLock джоба `task-scheduler` (ключ — `task-scheduler`, TTL — `harness.task.scheduler.ttl`, интервал — `harness.task.poll-interval`, по умолчанию ~5 с) SHALL: подбирать задачи с `(current_state_kind='AGENT' AND status_projection='RUNNING' AND нет STATE-сессии)` (bootstrap) и `(current_state_kind='WAIT_TASKS')` (переоценка; WAIT_WEBHOOK в выборку не входит — только таймаут-скан). Также: `task-timeout-scanner` под своим именем сканирует `deadline_at IS NOT NULL AND deadline_at <= now() AND status_projection IN ('RUNNING','WAITING')` → TIMEOUT. Verify: интеграционный тест POLL-bootstrap'а (EVENT-wake упал → POLL поднял); тест таймаут-скана.

## J. AGENT-state + STATE-сессии + metaTools

- [ ] J.1 Расширить `session` пакет: маппинг в `SessionEntity` полей `task_id`/`state_code`/`agent_revision_id` (колонки и PARTIAL UNIQUE `(task_id, state_code) WHERE kind='STATE'` уже в миграции 005 — новых миграций не нужно, индекс bootstrap-скана — H.1); `WrongSessionKindException` (уже есть); `StateSessionService` (`findOrCreate(taskId, stateCode, agentRevisionId)` — UPSERT-логика по PARTIAL UNIQUE; создание **атомарно**: в одной транзакции insert сессии + seed-`SYSTEM`-сообщение + `last_seq` — execution-model §7.2). Verify: unit-тест UPSERT + атомарности (состояние «сессия есть, seed не записан» невозможно).
- [ ] J.2 Расширить `AgentTurnEngine` (`execution/impl/`): применение `transition` — транзакционно **в момент исполнения tool-call** (CAS `current_state` + INSERT `task_transition_history` атомарны с исполнением инструмента; write-ahead не делает отложенную запись transition на turn-finish). Гейт metaTools (D-52/D-59): разрешён только при `instructionSource = USER`; plumbing — `Caller.instructionSource()`, `sessionOwner()` проставляются во всех точках входа; precondition в `AgentTurnEngine.executeToolCall`. Verify: интеграционный тест — сообщение USER → агент вызывает transition → запись в истории в момент tool-call; сообщение TOOL_RESULT → transition заблокирован (тест на гейт).
- [ ] J.3 Реализовать `AgentStateBootstrapper` (`execution/impl/`): `bootstrap(taskId, stateCode)` — вызывается из `task-scheduler` для AGENT-state без сессии: `findOrCreate` STATE-сессии → создать wake-событие для `sess-{sessionId}` (через `session-wake-bus` из M1). Verify: интеграционный тест «AGENT-state без сессии → bootstrap → Turn стартовал».
- [ ] J.4 Реализовать `TaskWakeBroadcaster` (SSE `tasks/{id}/events` — задачный канал; Javadoc: это SSE-широковещатель события задачи, **не** wake-шина движка — см. I.5): аналог M1 `InMemorySessionEventBroadcaster`. События: `task.transition { id (TransitionDto.id), taskId, fromState, toState, kind, reason, createdAt }`, `task.status { taskId, currentState, statusProjection, suspended }`, `subtask.terminal { taskId, terminalTaskId, terminalStatus }`, `task.comment { id, taskId, body, author, createdAt }`. Курсор — монотонный `task_event_seq` (все события задачи нумеруются, включая не-transition); реализовать инкремент durable-счётчика `task.task_event_seq` транзакционно с эмиссией каждого события (переживает рестарт; data-model §4); snapshot при коннекте = последний `task.status`; реконнект по Last-Event-ID добирает историю без дублей. SSE-контроллер на сгенерированном интерфейсе (api-contracts §3.2). Verify: интеграционный тест SSE — клиент подключается, видит snapshot + события; реконнект с курсором — без пропусков.

## K. Suspend/Stop + комментарии/дерево/зависимости + REST

- [ ] K.1 Реализовать REST `tasks`, `tasks/{id}/{subtasks,suspend,resume,stop,dependencies,history,comments,tree}` контроллеры на сгенерированных интерфейсах (пачка D — генерация). Подзадачи: `parent_task_id` + наследование `owner_user_id` от породившей сессии (через `Caller.sessionOwner()` в `ApiMappers`). Комментарии: append-only, `author_user_id = JWT principal` или `NULL + агент-пометка` (для агентских — из `Caller.instructionSource()`). История: конверт `{ items, nextCursor }`; курсор — пара `(created_at, id)` записи `task_transition_history`. Дерево: BFS-обход по `parent_task_id` (без лимита depth → всё поддерево, или depth=N). Покрытие `GET /api/v1/sessions/{id}/tree` (`taskId`+`stateCode` для STATE-узлов) — через регенерацию D.2 (обновление контроллера сгенерированным интерфейсом). Verify: интеграционные тесты через сгенерированный клиент.
- [ ] K.2 Suspend/Resume/Stop на сгенерированных интерфейсах: `POST /tasks/{id}/suspend { cascade } → 204` (флаг); `POST /tasks/{id}/resume → 204` — снять suspended для нетерминальной **и опубликовать `task-wake`** (InProcessTaskWakeBus → переоценка/bootstrap подхватывают; `409 task-already-terminal` для терминальной); `POST /tasks/{id}/stop → 202` — **всегда каскадный** (без параметра `cascade`): suspend + отмена Turn'ов задачи и подзадач + `'$CANCELLED'`. `Suspend` идемпотентен. Verify: интеграционные тесты по контракту (idempotency, 409 на терминале, resume → wake).
- [ ] K.3 Зависимости на сгенерированных интерфейсах: `POST /tasks/{id}/dependencies { blockedBy:[id] } → 201`; `DELETE /tasks/{id}/dependencies/{blockerId} → 204`. DFS-валидация циклов (см. H.3) → `422 dependency-invalid` с errors[]. После добавления ребра — InProcessTaskWakeBus событие «blocked-changed» для переоценки WAIT_TASKS заинтересованных задач. Verify: unit-тест цикл-валидации (3 задачи → цикл → 422); интеграционный тест «зависимость разблокировала WAIT_TASKS».

## L. Триггеры + вебхуки

- [ ] L.1 Реализовать `TriggerRegistry` (внутренняя деталь пакета `task`, `impl/TriggerRegistryImpl`): `create(name, workflowKey, rev?, params, tags, owner) → Trigger { id, url, … }`; `list(...)` (cursor-пагинация по `createdAt desc`); `revoke(id) → boolean` (`UPDATE SET revoked_at = now()`). `url = ${harness.webhook.base-url}/api/webhooks/triggers/{triggerId}/{token}`, где `token = HMAC-SHA256(server_secret, "trigger:"+triggerId)`. Verify: unit-тесты revoke+HMAC.
- [ ] L.2 Реализовать `WebhookSignatureVerifier` (чистая функция): `verify(kind, entityId, token) → boolean` (`HMAC-SHA256(server_secret, kind + ':' + entityId)`). Используется и в `tasks`, и в `triggers`. Verify: unit-тест positive/negative (битый токен → false).
- [ ] L.3 Реализовать `WebhookHandlers` (`api/impl/` — HTTP-контроллеры api; делегация в `task` через `TaskRegistry`): `handleTaskWebhook(taskId, token, source, body)` — verify → load task → если `current_state.type = WAIT_WEBHOOK` → валидация `payloadSchema` (ограниченный профиль D-58) → CAS переход NEXT (или `202` + ERROR при провале валидации); иначе `409 task-not-waiting-webhook`; кривой токен → `401 signature-invalid`; `handleTriggerWebhook(triggerId, token)` — verify → load trigger → если `revoked_at IS NULL` → создать задачу (через `TaskRegistry.createTask`) + `202 { taskId }`; иначе `410 trigger-revoked`. Контроллер `WebhookController` на сгенерированном интерфейсе (без `/v1`, без JWT). Verify: интеграционный тест всех путей (positive/negative, включая невалидный payload → 202 + ERROR).
- [ ] L.4 REST `triggers` на сгенерированном интерфейсе: `POST /triggers`, `GET /triggers`, `DELETE /triggers/{id}` (revoke). Verify: интеграционный тест через сгенерированный клиент.

## M. Приёмка M2

- [ ] M.1 ArchUnit-расширение: правила слоёв (`execution → {task, session, workflow}`), allowed-layers для `api` дополняется `task`/`workflow` — `api mayOnlyAccessLayers("execution", "intelligence", "session", "identity", "task", "workflow")` (иначе контроллеры задач/вебхуков падают на CI), запрет чужих `.impl` (4 модуля), циклы; новые тесты в `tests/architecture/ArchitectureRulesTest.java` — параметризованы по `workflow`/`task`. Verify: `mvn verify` ArchUnit-блок зелёный; намеренное нарушение (импорт чужого `.impl` в тестовом классе) ловится.
- [ ] M.2 Приёмочный e2e M2 (api-contracts/roadmap критерий): сценарий «двухфазное ревью с возвратом» на тестовом графе: `plan` (AGENT) → `bash:run-checks` (BASH) → `reviewer-1` (AGENT) → `reviewer-2` (AGENT) → `merge` (BASH). Цикл: при ошибке review → возврат в `plan`. Агент переводит задачу инструментом `transition` с reason. Тест с живым Keycloak, WireMock-LLM, реальным helper-контейнером. Проверки: состояния проходят в порядке; bash таймаут → TIMEOUT; FAILED reviewer-1 → цикл в `plan`; итоговый `TERMINAL: SUCCESS`. Verify: один интеграционный тест `acceptanceTwoPhaseReviewWithReturn`.
- [ ] M.3 Полный `mvn clean verify`; перенос решений D-47…D-58 в `docs/design/decisions.md` (формат ADR: решение → альтернативы → почему) + проверка наличия D-59 (D-59 уже добавлен — не дублировать при переносе); обновление `apply-notes.md` (секция «Пачка M2»): задачи D.1–M.3, критерий M2 выполнен, число тестов, отклонения и решения. Обновление AGENTS.md (состояние: M2 завершён). Коммит оркестратора.

## N. Архивирование M2

- [ ] N.1 `openspec archive m2-workflow-engine --yes` → спеки синхронизируются в `openspec/specs/{workflow-engine,task-engine,inbound-triggers,session-api,agent-turn,session-store,workspace-tools}/spec.md`. Commit + push.
