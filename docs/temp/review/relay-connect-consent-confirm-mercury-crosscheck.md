# Кросс-чек: relay-connect-consent-confirm (Mercury)

**Дата:** 2026-09-24  
**Второй ревьюер:** DeepSeek (reject)  
**Мой первоначальный вердикт:** approve

---

## Анализ претензий второго ревьюера

### F1 (MAJOR): При «Отклонить» статус показывает «подключён» вместо причины

**Проверка:**
- `relay-client.ts:262-267`: при `!allowed` register() возвращает `{ok:false}` и **не эмитит status**
- `ChatView.vue:45-56`: при `connected:true, registered:false, reason===undefined` → `if (s.connected) return 'подключён'`
- `desktop-chat/spec.md:23-26`: "в строке релея видна причина, а не безликое «подключён»"
- `desktop-relay-client/spec.md:25`: "после «отклонить» — причина видна в UI"

**Вердикт:** ✅ **Согласен** — это нарушение delta-спеки.
- Требование спеки: "причина последней неудачи регистрации (отказ согласования...) SHALL быть видна в строке релея"
- Факт: после decline reason не эмитится → UI показывает «подключён»
- Мои выводы в первоначальном отчёте были неверны: я сказал "minor, не баг", но это явное требование спеки

### F2 (MINOR): STATE-сессия не показывает индикатор

**Проверка:**
- `useRelay.ts:72`: `if (kind === 'STATE') return;` — тихо выходит
- `relayLabel` не учитывает `activeSession.kind`
- `desktop-relay-client/spec.md:67-70`: "пользователю показывается, что релей доступен только для root-сессий"

**Вердикт:** ✅ **Согласен** — спецификация требует сообщения, код этого не делает.

### F3 (MINOR): Параллельные регистрации

**Проверка:**
- `relay-client.ts:273-275`: параллельный register возвращает тот же promise
- `consentResolvers.set` перезаписывает
- `autoRequested` глобальный

**Вердикт:** ✅ **Согласен** — это известное ограничение, задокументировано разработчиком, не требует блокировки.

### F4 (MINOR): smoke.md устарел

**Проверка:**
- `web-desktop/docs/smoke.md` не обновлен после e2e-переработки

**Вердикт:** ✅ **Согласен** — документация не синхронизирована, minor issue.

---

## Итоговый вердикт

`reject`

**Почему:** F1 — нарушение delta-спеки (`desktop-chat/spec.md:23-26`, `desktop-relay-client/spec.md:25`): после decline причина не эмитится через status, UI показывает «подключён» вместо причины. Это требование spe-изменений change'а, не покрытие тестами.

F2-F4 — minor, могут быть исправлены после мержа.
